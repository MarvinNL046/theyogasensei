node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.local*
.nitro
.superpowers
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.playwright-mcp

# Auto-generated artefacts
public/sitemap.xml
# NOTE: routeTree.gen.ts is committed — Vercel runs `tsc --noEmit` (typecheck)
# before vite generates it, so a clean checkout needs it present or the build fails.
# It is regenerated by vite build; re-commit if routes change.


.vercel
